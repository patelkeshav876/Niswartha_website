const fs = require('fs');
const path = require('path');

// We can also handle transparency dynamically in browser HTML5 Canvas when the page loads!
// Let's create a browser utility function to remove white background from car image on the fly as well as process f1-car.png directly.
console.log('Script initialized.');
